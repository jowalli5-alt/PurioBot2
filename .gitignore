# База данных — НИКОГДА не должна попадать в git, иначе при обновлении
# бота командой `git pull` она будет затёрта версией из репозитория.
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Секреты
.env

# Python
__pycache__/
*.pyc
venv/
.venv/

# Логи, если вдруг пишутся в файл
*.log
